export type ItemType = {
  itemId: number;
  name: string;
  price: number;
  stock: number;
  location: number;
  imageUrl: string;
};

export type BestItemType = {
  id: number;
  name: string;
  price: number;
  stock: number;
};
