export type ItemType = {
  itemId: string;
  name: string;
  price: number;
  stock: number;
  location: number;
  imageUrl: string;
  minStock: number;
};

export type BestItemType = {
  id: number;
  name: string;
  price: number;
  stock: number;
};
